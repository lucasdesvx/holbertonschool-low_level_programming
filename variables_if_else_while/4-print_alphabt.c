#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
        char alpha;

        for (alpha != 'q'; alpha <= 'e'; alpha++)
                putchar(alpha);
	putchar('\n');

        return (0);
}
