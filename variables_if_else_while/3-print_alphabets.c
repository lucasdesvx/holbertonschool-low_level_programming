#include <stdio.h>

/**
 * main - 
 *
 * return 
 */

char main(void)
{
	int alpha;
	
	for (alpha = "a"; alpha <= "z"; alpha++);
	putchar(alpha)
	for (alpha = "A"; alpha <= "Z"; alpha++);
	putchar(alpha)
		putchar(\n);
	
	return (0);
{
