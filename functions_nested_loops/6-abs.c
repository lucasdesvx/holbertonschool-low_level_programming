#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @absolute:
 *
 * Return: to _abs
 */

int _abs(int absolute)
{
	if (absolute < 0)
		return (-absolute);
	else
		return (absolute);
}
