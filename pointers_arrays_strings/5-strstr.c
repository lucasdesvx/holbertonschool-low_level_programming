#include "main.h"

/**
 *
 *
 *
 *
 *
 *
 */

char *_strstr(char *haystack, char *needle)
{
	char *lestring1 = haystack;
	char *lestring2 = needle;
	
	while (*haystack)
	{
		while (*n && *h == *n)
			h++;
			n++;
		}
	if (!*n)
		return (haystack);

		haystack++;
	}

	return (0);
}
